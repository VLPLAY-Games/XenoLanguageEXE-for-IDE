# Xeno Language EXE для Windows IDE

## 🏗️ Обзор проекта

Этот репозиторий предоставляет версию языка программирования Xeno в виде исполняемого файла для Windows, специально разработанную для использования с **Xeno IDE**. Он создает мост совместимости, который позволяет коду Xeno на основе Arduino работать нативно на системах Windows.

## 🌉 Архитектура моста

### Трехуровневая система моста

#### 1. **Слой совместимости Arduino-Windows** (`arduino_compat.h/cpp`)
- **Назначение**: Эмулирует Arduino API на Windows
- **Компоненты**:
  - Класс `XenoString` заменяет Arduino `String`
  - Эмуляция Serial с очередями ввода/вывода
  - Заглушки функций GPIO (`pinMode`, `digitalWrite` и т.д.)
  - Функции времени (`delay`, `millis`)

#### 2. **Адаптация исходного кода языка** (`auto_xeno_bridge.py`)
- **Назначение**: Автоматически преобразует код Xeno для ESP32 в код, совместимый с Windows
- **Возможности**:
  - Заменяет включения `Arduino.h` на `arduino_compat.h`
  - Преобразует `String` в `XenoString` с помощью макросов
  - Обрабатывает конфликты функций (например, `isInteger`)
  - Обрабатывает целые деревья каталогов

#### 3. **Слой связи Windows-IDE** (`xeno_host.cpp`)
- **Назначение**: Предоставляет интерфейс исполняемого файла для Xeno IDE
- **Возможности**:
  - Стандартный протокол ввода/вывода с IDE
  - Фоновое выполнение VM
  - Управление компиляцией и выполнением
  - Отчетность информации о версиях

## 📁 Структура файлов
```
XenoLanguageEXE-for-IDE/
├── arduino_compat.h # Заголовок эмуляции Arduino API
├── arduino_compat.cpp # Реализация Arduino API
├── auto_xeno_bridge.py # Скрипт автоматического преобразования кода
├── xeno_host.cpp # Основной исходный код исполняемого файла
├── CMakeLists.txt # Конфигурация сборки
├── version.rc # Файл ресурсов версии
└── Основные файлы языка Xeno
```

## 🔧 Сборка из исходного кода

### Предварительные требования
- CMake 3.10+
- Visual Studio 2017+ или MinGW-w64
- Компилятор с поддержкой C++17

### Шаги сборки
```bash
Настройка проекта:
cmake -B build -G "Visual Studio 17 2022" -A x64

Сборка релизной версии:
cmake --build build --config Release

Вывод: build/Release/xeno_host.exe
```

## 🔄 Интеграция с Xeno IDE

Xeno IDE автоматически загружает и использует `xeno_host.exe` из релизов этого репозитория. Процесс интеграции:

1. **Обнаружение IDE**: IDE проверяет `xeno/xeno_info.txt` на совместимость версий
2. **Протокол связи**: IDE общается через стандартный ввод/вывод
3. **Поток выполнения**:
   - IDE отправляет команду `COMPILE` с исходным кодом
   - `xeno_host.exe` компилирует с использованием ядра языка Xeno
   - IDE отправляет команду `RUN` для выполнения в фоновом потоке
   - Ввод/вывод в реальном времени через эмуляцию Serial

## 🎯 Основные возможности

### Кросс-платформенная совместимость
- **Эмуляция Arduino API**: Полная поддержка Serial, String и базовых функций GPIO
- **ESP32 в Windows**: Бесшовная миграция кода между платформами
- **Выполнение в реальном времени**: Фоновая VM с управлением потоками

### Интеграция с IDE
- **Стандартный протокол**: Простое построчное общение
- **Управление версиями**: Автоматические уведомления об обновлениях
- **Обработка ошибок**: Всесторонняя отчетность об исключениях
- **Управление состоянием**: Управление запуском/остановкой/пошаговым выполнением

### Производительность и безопасность
- **Ограничения инструкций**: Настраиваемое максимальное количество шагов выполнения
- **Безопасность памяти**: Ограниченные строковые операции и лимиты стека
- **Изоляция процессов**: Отдельный процесс VM от IDE

## 🔗 Связанные проекты

- **Основной язык Xeno** (ESP32): [Репозиторий Xeno-Language](https://github.com/VLPLAY-Games/Xeno-Language)
  - Основная реализация языка для микроконтроллеров ESP32
  - Исходная основа для этой адаптации Windows

- **Xeno IDE**: [Репозиторий XenoIDE](https://github.com/VLPLAY-Games/XenoLanguageIDE)
  - Интегрированная среда разработки для Windows
  - Основной потребитель этого исполняемого файла

## 📥 Установка для пользователей IDE

### Автоматическая установка (Рекомендуется)
1. Откройте Xeno IDE
2. Перейдите в Настройки → вкладка Xeno
3. Нажмите "Check Updates"
4. Выберите версию и нажмите "Install"

### Ручная установка
1. Скачайте `xeno_host.exe` из раздела Releases
2. Поместите в директорию `xeno/` относительно IDE
3. Важно: Запустите `xeno_host.exe` один раз для генерации конфигурации
4. Перезапустите IDE

## 🛠️ Использование для разработки

### Запуск скрипта преобразования
Преобразование исходных файлов языка Xeno:
```
python3 auto_xeno_bridge.py
```

### Включенные исходные файлы
Сборка включает все необходимые основные файлы языка Xeno:
- xeno_common.cpp - Общие утилиты и структуры данных
- xeno_compiler.cpp - Компиляция и парсинг кода
- xeno_vm.cpp - Механизм выполнения виртуальной машины
- xeno_security.cpp - Лимиты безопасности и валидация
- XenoLanguage.cpp - Основной API интерфейс

## 📋 Поддерживаемые команды

| Команда | Параметры | Описание |
|---------|------------|-------------|
| COMPILE | Исходный код + длина | Компиляция программы Xeno |
| RUN | Нет | Выполнение в фоне |
| STOP | Нет | Остановка выполнения |
| STEP | Нет | Выполнение одной инструкции |
| STDIN <данные> | Входная строка | Отправка в очередь Serial |
| SET_MAX_INSTRUCTIONS | Число | Изменение лимита выполнения |

## 🔄 Совместимость версий

| Язык Xeno | Версия моста | Версия IDE | Уровень поддержки IDE |
|---------------|----------------|-------------|---------------|
| 0.1.4+ | 0.1.4.1+ | 1.0.0+ | Полная поддержка |
| 0.1.3 | 0.1.3.1 | 1.0.0+ | Ограниченная поддержка с ограничениями |
| < 0.1.3 | - | - | Не поддерживается |

## 🐛 Решение проблем

### Распространенные проблемы

**"xeno_host.exe не найден"**
- Выполните шаги ручной установки
- Убедитесь, что файл находится в директории `xeno/`
- Проверьте, не заблокировал ли файл Windows Defender

**"Ошибка установки" после ручной настройки**
- Запустите `xeno_host.exe` напрямую один раз
- Убедитесь, что `xeno_info.txt` сгенерирован
- Проверьте права доступа к директории

**Не работает ввод Serial**
- Убедитесь, что код ожидает промпт `INPUT`
- Используйте `Serial.readString()` в коде Xeno
- Проверьте, что VM запущена перед отправкой ввода

**Ошибки сборки**
- Проверьте версию CMake 3.10+
- Проверьте установку Visual Studio
- Убедитесь, что установлен Windows SDK

## 📄 Лицензия

Этот проект лицензирован под Apache License 2.0 - смотрите файл [LICENSE](LICENSE) для деталей.

## 🤝 Участие

Я приветствую участие в улучшении слоя совместимости Windows:

1. Сообщайте о пробелах в покрытии Arduino API
2. Улучшайте производительность эмуляции Serial
3. Добавляйте оптимизации для Windows
4. Улучшайте обработку ошибок и диагностику

## 📞 Поддержка

Для проблем с исполняемым файлом Windows:
1. Проверьте [основной репозиторий языка](https://github.com/VLPLAY-Games/Xeno-Language) для проблем с языком
2. Создайте issue в этом репозитории для проблем совместимости с Windows
3. Проверьте консоль IDE для детальных сообщений об ошибках
4. Проверьте совместимость версий между всеми компонентами